---
description: Review the current diff, apply confirmed fixes, update the test plan, and run the tests
---
Review the current diff, apply any CONFIRMED findings as fixes, write/update a test plan covering the changes, then run the tests. If tests fail or the fixes touched new code, run another review pass. Stop when review finds nothing CONFIRMED and all tests pass.
