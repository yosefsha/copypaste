[project]
name = "copypaste-cdk"
version = "0.1.0"
description = "AWS CDK app for deploying copypaste infrastructure"
requires-python = ">=3.13"
dependencies = [
    "aws-cdk-lib>=2.170.0",
    "constructs>=10.0.0",
]

[tool.uv]
package = false
